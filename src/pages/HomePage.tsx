// src/pages/HomePage.tsx
import React from 'react';

const HomePage: React.FC = () => {
  return (
    <section aria-labelledby='home-title'>
      <h1 id='home-title' style={{ marginBottom: '1rem' }}>
        HomePage
      </h1>
      <p>Estrutura base pronta.</p>
    </section>
  );
};

export default HomePage;
